#define T tolower
#define TT toupper
#define TTT toascii
void main(int argc,char *argv[]){
    struct point{
        char t1,t2;
    }
    function __DATE__(){
    return sin(T + t1) + cos(T + t2);
    return sin(TT - t1) + cos(TT - t2);
    }
    struct rect{
        char t3,t4;
    }
    function __TIME__(){
    return asin(T + t3) + acos(T + t4);
    return asin(TTT - t3) + acos(TTT - t4);
    }
    struct cube{
        char t5,t6;
    }
    function __DATE__TIME__(){ 
    return tan(TT + t5) + atan(TTT + t6);
    return tan(TTT - t5) + atan(TT - t6);
    }
}

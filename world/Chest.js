
import "color.css"
caches = new Promise((resolve, reject) => {
const black={WebGL}
const grey={WebGLQuery}
const white={WebGLBuffer}
var Blue_Chest={
   WebGLContextEvent, WebGLRenderingContext, WebGL2RenderingContext
}
var Green_Chest={
   WakeLock, WakeLockSentinel
}
let anti_Chest={
   WeakMap, WeakSet
}
})
